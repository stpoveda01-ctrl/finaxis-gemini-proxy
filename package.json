{
  "name": "finaxis-gemini-proxy",
  "version": "1.0.0",
  "description": "Proxy serverless para conectar FinAxis con la API de Google Gemini",
  "private": true,
  "engines": {
    "node": ">=20"
  }
}
